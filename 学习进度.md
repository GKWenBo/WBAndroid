# 学习进度（单一真源 · 每次学习必更新）

> 规则：开始会话先读本节定位 `⬜`/`🔄`；结束会话更新总览表 + 追加当日日志 + 写 next step。
> 兜底：关键摘要同时写入 `.workbuddy/memory/YYYY-MM-DD.md`，即使本文件误删也能恢复。

## 总览表
| 阶段 | Demo | 状态 | 完成日 |
|---|---|---|---|
| W1 Kotlin + 环境 | demo_02 | ✅ 完成 | 2026-07-11 |
| W2 Compose UI | demo_03 | 🔄 进行中 | — |
| W3 架构与数据 | demo_04 | ⬜ 未开始 | — |
| W4 实战整合 | demo_05 | ⬜ 未开始 | — |

图例：✅ 完成 / 🔄 进行中 / ⬜ 未开始

## 日期日志
### 2026-07-09
- **完成**：路线图与进度机制定稿；建立仓库总纲 `学习路线.md`、本进度文件 `学习进度.md`。
- **完成**：Week 1 骨架 `Demo/demo_02_kotlin_basics/` —— 标准 Kotlin Android 工程（含 Kotlin 插件 + 协程依赖），附 `KotlinPlayground.kt`（val/var、data/sealed class、扩展函数、空安全、作用域函数、lambda）与 `CoroutineExercises.kt`（async 并发合并，对应 Week1 检查点）。
- **卡点/已修复（两次）**：
  1. 首报 `already on the classpath with an unknown version` —— 因显式指定 Kotlin 插件版本 2.3.20 与注入版本冲突。改为不写版本。
  2. 二报 `Cannot add extension with name 'kotlin'` —— 即便不写版本，显式 `id("org.jetbrains.kotlin.android")` 仍会与 AGP 自动应用的 Kotlin 插件重复。最终修复：**完全删除模块里的 Kotlin 插件声明**（对齐 `demo_01` 的可用写法，AGP 9.2.1 自动注入并应用 Kotlin）。协程仅作为普通库依赖 `libs.androidx.coroutines` 引入。沙箱无 Android SDK，仍需你在本地重新 Sync 验证。
- **下一步**：在本地 Android Studio 打开 `demo_02` 跑通，确认 Logcat 出现 `KotlinDemo` 7 段练习 + 协程合并结果；随后进入 `demo_03`（Compose UI）。

### 2026-07-11
- **验证通过**：`demo_02` 在本地 Android Studio 实机跑通，Logcat `KotlinDemo` 输出 7 段 Kotlin 练习 + 协程并发合并结果。W1 正式完成。
- **经验确认**：本机 AGP 9.2.1 自动注入并应用 Kotlin 插件，模块 `build.gradle.kts` **不得声明** `id("org.jetbrains.kotlin.android")`（否则 `extension 'kotlin' already registered`）。后续 demo_03/04/05 一律沿用。
- **开始 W2**：构建 `Demo/demo_03_compose_ui/`（Compose 声明式 UI + 导航 + 深色模式），沿用 demo_02 的 gradle 骨架，新增 `kotlin.compose` 插件、Compose BOM 与 material3/navigation-compose 等依赖。

## next step
在本地 Android Studio 打开 `demo_03` 跑通（登录页→列表页导航 + 深色模式切换）→ 标记 W2 完成，进入 `demo_04`（MVVM + 网络 + Room）。
