# 2026-07-16 工作日志

- **提交代码（用户指令"提交代码"）**：`git commit` → `7559420`，335 文件 / +12470 行。
  范围：W3 `Demo/demo_04`（MVVM+Retrofit+Room(KSP)+Koin+Coil）、W4 `Demo/demo_05`（GitHub 用户搜索实战）、`学习路线.md`/`学习进度.md` 更新、KMP 教学资料（official-sample、doc、README）。
- **待用户决策的历史遗留**：`KMP/MyKotlinProject` 与 `KMP/iOSApp` 目录磁盘已删，但此前被 `git add` 进暂存区，本次按"提交暂存全部"语义写入历史（提交后变 `deleted` 状态）。两种处理：①保留→`git checkout HEAD -- KMP/MyKotlinProject KMP/iOSApp` 恢复工作区；②移除→`git rm --cached` 两目录 + 补删除提交。等用户定，未擅自操作。
- **未推送**：仅"提交"未"推送"；本地领先 `origin/main` 2 个提交（含前次 `503a541`）。未跟踪 `KMP/my-sample/` 未纳入本次提交。
