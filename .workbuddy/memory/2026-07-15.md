# 2026-07-15 学习记录

- **一次性交付 W2–W4 全部课程代码**（用户："一次性完成所有教学课程，后面有问题再沟通"）。
  - W2 demo_03（Compose UI）此前已交付。
  - W3 demo_04（MVVM + Retrofit + Room + Koin + Coil，Rick and Morty API）。
  - W4 demo_05（GitHub 用户搜索综合 App + 权限/调试/打包说明）。
- **技术栈版本已查证（2026-07 真实兼容，避免踩版本坑）**：
  - Kotlin 2.3.20 真实存在（2026-03-16）；Kotlin 2.4.0 为 2026-06 新版。
  - KSP 2.3.10（须匹配 Kotlin 2.3.x 前缀）。
  - Retrofit 2.11.0 / OkHttp 4.12.0 / kotlinx-serialization-json 1.10.0 / Room 2.6.1 / Koin 4.2.2 / Coil 3.5.0。
  - Room 3.0（androidx.room3）已发布但新手勿用（包名/API 大改），用 2.6.1。
- **固化约束（跨 demo 一致，不再踩坑）**：
  1. AGP 9.2.1 自动应用 Kotlin 插件 → 模块 build.gradle.kts 不声明 `kotlin.android`。
  2. Kotlin/Compose/KSP 版本统一在 `settings.gradle.kts` 的 `pluginManagement` 声明。
  3. Room 用 KSP（非 kapt）；KSP 版本匹配 Kotlin。
  4. Koin 取 ViewModel 用 `koinViewModel()`（非标准 `viewModel()`）。
  5. 用 `collectAsStateWithLifecycle`（lifecycle-runtime-compose）而非 `collectAsState`。
- **待办**：用户在本地 Android Studio 逐一 Sync + Run 验证 demo_03/04/05（沙箱无 Android SDK，无法实机构建）。
- 各 Demo 的 README.md 含检查点与 iOS 工程师对照坑位。
