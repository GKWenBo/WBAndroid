# WanReader 教学进度（单一真源 · 每节课必更新）

> 规则：会话开始先读本文件定位 `⬜`/`🔄`；会话结束更新总览表 + 追加当日日志 + 写 next step。
> 本进度独立于仓库根目录的 `学习进度.md`（原 4 周路线），互不影响。
> 设计文档：`docs/superpowers/specs/2026-07-12-wanreader-teaching-design.md`

## 总览表

| # | 阶段 | 状态 | 完成日 | 教学文档 |
|---|---|---|---|---|
| S0 | 工程初始化与配置 | ✅ 完成 | 2026-07-13 | doc/S0_工程初始化与配置.md |
| S1 | 架构骨架（分层 + Hilt + 导航） | ✅ 完成 | 2026-09-16 | doc/S1_架构骨架与Hilt.md |
| S2 | 网络层（Retrofit + 统一封装） | 🔄 待真机验收 | — | doc/S2_网络层与错误处理.md |
| S3 | 首页列表（Paging 3 + 三态） | ⬜ | — | — |
| S4 | 登录与会话（Cookie + DataStore） | ⬜ | — | — |
| S5 | 详情与收藏（WebView + 乐观更新） | ⬜ | — | — |
| S6 | 本地缓存（Room + 离线优先） | ⬜ | — | — |
| S7 | XML/View 专题 | ⬜ | — | — |
| S8 | 多模块演进 | ⬜ | — | — |
| S9 | 构建变体与多渠道 | ⬜ | — | — |
| S10 | 分发与上架 | ⬜ | — | — |

图例：✅ 完成 / 🔄 进行中 / ⬜ 未开始

## 日期日志

### 2026-07-13
- **完成**：教学设计与 S0 实施计划定稿并提交（11 阶段方案 A，设计文档见上方链接）。
- **完成**：S0 交付——`WanReader/` 工程骨架（AGP 9.2.1 / Kotlin 2.3.20 / Gradle 9.4.1 / compileSdk 36，复用 demo_03 已验证组合）+ app 模块最小 Compose 模板 + 教学文档 `doc/S0_工程初始化与配置.md`。
- **现场踩坑并已修复**：仓库根 `.gitignore` 的 `*.jar` 规则静默吞掉 `gradle-wrapper.jar`，已加例外规则 `!**/gradle/wrapper/gradle-wrapper.jar` 并补提交；完整分析记入 S0 文档坑④。
- **预埋讲解的坑**：Kotlin 插件重复注入（坑①）/ 下载镜像（坑②）/ JDK 不匹配（坑③）。
- **验收通过**：学员本地 Android Studio Sync + 真机跑通，S0 正式完成。

### 2026-07-13（S1）
- **完成**：S1 交付——KSP 2.3.9 + Hilt 2.60.1 接入（版本已联网核实：Hilt 2.59 起支持 AGP 9；KSP1 不兼容 Kotlin 2.3+）；分层包结构（ui/di/data）；品牌蓝主题；底部三 Tab 导航；Home 页展示 Module→Repository→ViewModel→UI 完整注入链路。
- **教学文档**：doc/S1_架构骨架与Hilt.md（Hilt 五注解 iOS 视角拆解 + kapt/KSP 坑 + 排错套路）。
- **待办**：学员本地 Sync（首次下载 Hilt/KSP 依赖）+ 真机验收 S1 文档 checklist。

### 2026-09-16（S1 编译排错）
- **复现**：`:app:kspDebugKotlin` 报 `not a valid name: provideAppVersionName-Nwjfrp0`。
- **修复**：将 DI 边界的 `AppVersionName` 从 `@JvmInline value class` 改为 `data class`，避免 JVM 名称改编影响 Hilt 代码生成。
- **后续构建修复**：AndroidX Hilt 1.4.0 要求 compileSdk 37，与本工程 36.1 不符；调整为 1.3.0，并显式声明 Activity/Navigation/Lifecycle 版本，纠正它们由 Compose BOM 管理的错误注释。
- **验证**：本机 `./gradlew :app:assembleDebug --console=plain` 成功（41 个任务执行）；KSP 和 Hilt 代码生成通过。尚未进行设备运行验收，S1 保持待真机验收。
- **教学文档**：S1 文档新增 §5.1 / §5.2，记录实际错误、原因、修复及官方参考。

### 2026-09-16（S2 网络层）
- **S1 收官**：依据学员已勾选并提交的 S1 六项验收清单（0f06884），标记 S1 完成。
- **本课交付**：Retrofit + OkHttp + serialization；BaseResponse/NetResult；DTO 与业务模型映射；单例 Hilt 网络模块；公共 Accept 头、超时及 Debug 日志；首页自动加载结果摘要和手动重试。
- **教学文档**：`doc/S2_网络层与错误处理.md`，包含 iOS 对照、文件导航、错误分类、JSON 缺失/null、协程取消、线程、HTTPS、日志与验收清单。
- **验证**：首次测试因尚未实现网络接口而编译失败；实现后 10 项 ArticleRepositoryTest 全部通过（0 跳过/失败/错误）；`:app:assembleDebug` 成功；`:app:lintDebug` 为 0 errors、16 warnings；`git diff --check` 通过。
- **真实接口检查**：开发机只读 GET `https://wanandroid.com/article/list/0/json` 返回真实文章；这不等于 Android 设备端运行通过。
- **边界**：设备联网/断网/重试尚待学员验收；本课未实现完整列表/分页/登录，也未自动提交或推送。

## next step

学员阅读 S2 文档并运行：确认首页真实文章摘要、WanNetwork 日志、断网提示及恢复后重试 → 勾选 S2 清单 → 标记 S2 ✅ → 开始 S3（首页分页列表）。
